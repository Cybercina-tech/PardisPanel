# Frontend — Tailwind CSS

Build and watch from this folder:

```bash
npm install
npm run build-css    # one-time build → ../static/dist/output.css
npm run watch-css    # watch for changes
```

Output is written to `../static/dist/output.css` (Django static files).
